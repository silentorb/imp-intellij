import silentorb.mythic.generation.texturing.*

let dimensions = Dimensions 512 512

let output = perlinNoiseColor
    dimensions = dimensions
    scale = 0.1
    octaves = 10
    roughness = 0.5
    firstColor = (SolidColor 0.5 1.0 0.4)
    secondColor = (SolidColor 0.0 0.0 0.0)
